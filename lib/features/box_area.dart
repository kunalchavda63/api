import 'package:api/core/app_ui/app_ui.dart';

class BoxArea extends StatelessWidget {
  const BoxArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
